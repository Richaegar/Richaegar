#include <stdio.h>

int main() {
    int number; // Declare an integer variable named 'number'

    number = 50; // Assign the value 50 to the variable

    printf ("the value is: %d/n", number); // Print the value using printf

    return 0;
}
